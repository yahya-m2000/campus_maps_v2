// theme/styles.js
import {StyleSheet} from 'react-native';

export const createStyles = theme =>
  StyleSheet.create({
    component: {
      // Use theme properties here
    },
    container: {
      // Use theme properties here
    },
    // ... other style definitions
  });

backgroundColourn;
